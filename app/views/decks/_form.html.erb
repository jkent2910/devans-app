<%= simple_form_for(@deck, html: {class: 'form-horizontal'}) do |f| %>
    <%= f.input :name %>
    <%= f.input :category, collection: Deck::CATEGORIES %>
    <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>