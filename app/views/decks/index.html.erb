<% @decks.each do |d| %>
    <%= d.name %>
    <%= link_to "Delete", deck_path(d), method: :delete, class: "btn btn-danger" %>
    <%= link_to "Edit", edit_deck_path(d), class: "btn btn-success" %>
<% end %>